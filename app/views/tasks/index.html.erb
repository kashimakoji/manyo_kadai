<h1>タスク一覧</h1>
<%= button_to "タスク新規作成", new_task_path, method: :get %>
<hr>
<table>
  <tr>
    <th>名称</th>
    <th>詳細</th>
  </tr>
<% @tasks.each do |task| %>
  <tr>
    <td><%= task.task_name %></td>
    <td><%= task.content %></td>
  </tr>
<% end %>
</table>
